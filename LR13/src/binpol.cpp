#include "binpol.h"

#ifndef IOSTREAM_H
#define IOSTREAM_H
#include <iostream>
#endif

#ifndef MATH_H
#define MATH_H
#include <math.c>
#endif

void
BinPolynom:init(int order, char* coef)
{
  if( coef == NULL ) {
    order_ = order;
	coefficents_ = calloc(order / 8 + 1, 1);
	return;
  };

  if( 	)
};


